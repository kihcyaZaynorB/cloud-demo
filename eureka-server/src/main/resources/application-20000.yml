spring:
  application:
    name: eureka-server

server:
  port: 20000

eureka:
  instance:
    hostname: eureka.20000

  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka.20001:20001/eureka/